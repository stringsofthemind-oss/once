name: LIMBO-style adversarial retry benchmark

on:
  pull_request:
    paths:
      - 'examples/limbo-style-benchmark/**'
      - '.github/workflows/limbo-style-benchmark.yml'
  push:
    branches:
      - main
    paths:
      - 'examples/limbo-style-benchmark/**'
      - '.github/workflows/limbo-style-benchmark.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run adversarial retry benchmark
        run: python examples/limbo-style-benchmark/benchmark.py
