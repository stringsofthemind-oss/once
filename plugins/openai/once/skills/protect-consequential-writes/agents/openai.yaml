interface:
  display_name: "Once — Protect Consequential Writes"
  short_description: "Safe retries after ambiguous external writes"
  default_prompt: "Use $protect-consequential-writes to audit this project for ambiguous external writes, retry-after-unknown-outcome, idempotency gaps, reconciliation needs, and duplicate side-effect risk."

policy:
  allow_implicit_invocation: true
